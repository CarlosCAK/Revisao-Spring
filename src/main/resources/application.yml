spring:
  application:
    name: Revisao-Spring
  datasource:
    url: jdbc:mysql://localhost:3306/db_rev_spring?createDatabaseIfNotExist=true
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 9090
